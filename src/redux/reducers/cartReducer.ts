let initialState = {
    cartProducts: [
        
        
    ]
}
const cartReducer = (state = initialState, action:any) => {
    switch (action.type) {
        
        default: {
            return state
        }
    }
}


export default cartReducer;